#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,x,y;
    scanf("%d%d%d",&a,&x,&y);
    if(a%x==0 && a%y==0){
        printf("Yes.");
    }
    else
        printf("No.");
    return 0;
}
